var React = require('react');

var Footer = React.createClass({

  render: function() {
    return (
    	<footer className="centered">
    		footer info
    	</footer>
    );
  }
});

module.exports = Footer;